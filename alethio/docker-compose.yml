version: '3.7'

services:
  redis:
    container_name: memento-redis
    image: redis:5.0.5-alpine
    restart: always
    command: ["redis-server", "--appendonly", "yes"]
    # volumes:
    #   - .volumes/redis:/data
    networks:
      - node_network

  postgres:
    container_name: memento-postgres
    image: postgres:11.5-alpine
    restart: always
    environment:
      - POSTGRES_USER=memento
      - POSTGRES_DB=memento
      - POSTGRES_PASSWORD=password
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - node_network

  memento:
    container_name: memento
    image: alethio/memento:latest
    restart: always
    depends_on:
      - redis
      - postgres
    ports:
      - 3000:3000
      - 3001:3001
    environment:
      - PG_PASSWORD=password
    volumes:
      - .volumes/memento:/config:ro
    networks:
      - node_network

  explorer:
    container_name: memento-lite-explorer
    image: alethio/ethereum-lite-explorer:latest
    restart: always
    depends_on:
      - memento
    ports:
      - 580:80
    volumes:
      - .volumes/lite-explorer/config.json:/usr/share/nginx/html/config.json:ro
    networks:
      - node_network

  node0:
    image: openethereum/openethereum
    ports:
      - 8545:8545
      - 8546:8546
    volumes:
      - ./node0.toml:/home/openethereum/config.toml:ro
      - ./base-chainspec.json:/home/openethereum/chainspec.json:ro
      - ./node.pwds:/node.pwds
    command: "--config /home/openethereum/config.toml"
    networks:
      - node_network

  node1:
    image: openethereum/openethereum
    ports:
      - 8547:8545
      - 8548:8546
    volumes:
      - ./node1.toml:/home/openethereum/config.toml:ro
      - ./base-chainspec.json:/home/openethereum/chainspec.json:ro
      - ./node.pwds:/node.pwds
    command: "--config /home/openethereum/config.toml"
    networks:
      - node_network

networks: 
  node_network:

volumes: 
  postgres_data:
